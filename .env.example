# Expo public env vars are bundled at build time. Do not commit real keys.
# Supabase (required)
EXPO_PUBLIC_SUPABASE_URL="https://your-project-id.supabase.co"
EXPO_PUBLIC_SUPABASE_ANON_KEY="your_anon_key_here"

# Supabase service role (optional; tooling/seeding only, never ship in client)
SUPABASE_SERVICE_ROLE_KEY="your_service_role_key_here"

# Observability
EXPO_PUBLIC_SENTRY_DSN="https://public@sentry.example.com/1"
EXPO_PUBLIC_ENVIRONMENT="development" # e.g., development | staging | production

# Setup hints:
# 1) Copy this file to .env and fill in values.
# 2) Keep EXPO_PUBLIC_* keys safe; they are bundled into the client.
# 3) Keep SUPABASE_SERVICE_ROLE_KEY only for local scripts/seed data, not in builds.
